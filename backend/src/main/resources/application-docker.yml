spring:
  datasource:
    url: jdbc:mysql://mysql:3306/flash_sale?useSSL=false&allowPublicKeyRetrieval=true&characterEncoding=UTF-8

  data:
    redis:
      host: redis
      port: 6379

  kafka:
    bootstrap-servers: kafka:29092